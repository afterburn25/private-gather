@extends('layouts.app') @section('title','Manage Events') @section('content')<section class="shell section"><div class="panel-head"><div><div class="eyebrow">EVENTS</div><h1>Manage events</h1></div><a class="btn primary" href="{{route('tenant.events.create')}}">Create Event</a></div><div class="panel">@foreach($events as $event)<div class="list-row"><div><strong>{{ $event->title }}</strong><small>{{ $event->starts_at->format('M j Y g:i A') }} · {{ $event->status }} · {{ $event->visibility }}</small></div><div class="row-actions"><a class="btn" href="{{route('tenant.events.attendees',$event)}}">Attendees</a><a class="btn" href="{{route('tenant.events.edit',$event)}}">Edit</a></div></div>@endforeach</div>{{ $events->links() }}</section>@endsection
