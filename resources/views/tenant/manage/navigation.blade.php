@extends('layouts.app')
@section('title','Navigation')
@section('content')<div class="container section"><h1>Navigation</h1><div class="panel">@foreach($items as $item)<form class="row-between" method="post" action="{{route('tenant.navigation.update',$item)}}">@csrf @method('patch')<div class="inline-controls"><span class="pill">{{$item->location}}</span><input name="label" value="{{$item->label}}"><input name="url" value="{{$item->url}}"><input name="sort_order" type="number" value="{{$item->sort_order}}"><label><input type="checkbox" name="is_enabled" value="1" @checked($item->is_enabled)> Enabled</label><button>Save</button></div></form><form method="post" action="{{route('tenant.navigation.destroy',$item)}}">@csrf @method('delete')<button class="link danger">Delete</button></form>@endforeach</div><form class="panel form-inline" method="post" action="{{route('tenant.navigation.store')}}">@csrf<select name="location"><option>header</option><option>footer</option></select><input name="label" placeholder="Label" required><input name="url" placeholder="/page/about" required><input name="sort_order" type="number" value="10"><button class="button button-primary">Add Link</button></form></div>@endsection