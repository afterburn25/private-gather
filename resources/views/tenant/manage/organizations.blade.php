@extends('layouts.app') @section('title','Organizations') @section('content')<section class="shell section"><div class="panel-head"><div><div class="eyebrow">ORGANIZATIONS</div><h1>Your clubs & event brands</h1></div><a class="btn primary" href="{{ route('organizations.create') }}">Create organization</a></div><div class="cards">@foreach($tenants as $tenant)<article class="card"><span class="pill">{{ str_replace('_',' ',ucfirst($tenant->type)) }}</span><h3>{{ $tenant->name }}</h3><p>{{ $tenant->domains->firstWhere('is_primary',true)?->domain }}</p><a class="btn" href="https://{{ $tenant->domains->firstWhere('is_primary',true)?->domain }}/manage">Manage</a></article>@endforeach</div></section>@endsection
