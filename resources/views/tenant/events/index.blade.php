@extends('layouts.app')
@section('title','Events — '.$tenant->name)
@section('content')<section class="page-hero"><div class="container"><span class="eyebrow">{{strtoupper($tenant->name)}}</span><h1>Upcoming events</h1><form class="filter-bar"><input name="q" value="{{request('q')}}" placeholder="Search"><input name="category" value="{{request('category')}}" placeholder="Category"><button>Search</button></form></div></section><section class="section"><div class="container"><div class="event-grid">@forelse($events as $event)<article class="event-card"><div class="event-image"><span>{{$event->starts_at->format('M d')}}</span></div><div class="event-card-body"><span class="eyebrow">{{strtoupper($event->public_location_label?:$event->city?:'LOCATION TBA')}}</span><h3>{{$event->title}}</h3><p>{{$event->starts_at->format('l, F j · g:i A')}}</p><a class="text-link" href="{{route('events.show',$event)}}">View event →</a></div></article>@empty<div class="empty-state"><h2>No upcoming events</h2><p>Check back soon.</p></div>@endforelse</div>{{$events->links()}}</div></section>@endsection