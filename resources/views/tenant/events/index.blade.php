@extends('layouts.tenant-site')
@section('title','Events — '.$tenant->name)
@section('content')
<section class="pg-page-hero tenant-site-hero"><div class="tenant-shell"><span class="pg-eyebrow">{{strtoupper($tenant->name)}}</span><h1>Upcoming events</h1><p>Explore what this community has chosen to make discoverable to you.</p><form class="pg-card form-inline" method="get" action="{{route('site.events')}}" style="margin-top:22px"><input name="q" value="{{request('q')}}" placeholder="Search events"><input name="category" value="{{request('category')}}" placeholder="Category"><button class="tenant-button tenant-button-primary">Search</button></form></div></section>
<section class="pg-page"><div class="tenant-shell"><div class="pg-grid pg-grid-3">@forelse($events as $event)<article class="pg-card pg-card-interactive pg-event-card"><div class="pg-media"><div class="pg-media-overlay"><span class="pg-pill">{{$event->starts_at->format('M d')}}</span></div></div><div class="pg-event-body"><div class="pg-event-meta"><span class="pg-pill">{{$event->public_location_label?:$event->city?:'Location protected'}}</span>@if($event->category)<span class="pg-pill">{{$event->category}}</span>@endif</div><h3 class="pg-event-title">{{$event->title}}</h3><p class="pg-event-copy">{{$event->starts_at->format('l, F j · g:i A')}}</p><a class="text-link" href="{{route('events.show',$event)}}">View event →</a></div></article>@empty<div class="pg-empty"><h2>No upcoming events</h2><p>Check back soon.</p></div>@endforelse</div><div style="margin-top:28px">{{$events->links()}}</div></div></section>
@endsection
