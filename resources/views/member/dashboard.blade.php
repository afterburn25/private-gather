@extends('layouts.app') @section('title','Dashboard') @section('content')
<section class="shell section"><div class="eyebrow">MEMBER DASHBOARD</div><h1>Welcome, {{ $user->display_name ?: $user->name }}</h1><div class="stats"><div class="stat"><strong>{{ $rsvps->count() }}</strong><span>Recent RSVPs</span></div><div class="stat"><strong>{{ $rsvps->where('status','approved')->count() }}</strong><span>Approved</span></div><div class="stat"><strong>{{ $user->tenants->count() }}</strong><span>Organizations</span></div></div><div class="panel"><div class="panel-head"><h2>Your events</h2><a href="{{route('site.events')}}">Discover events</a></div>@forelse($rsvps as $rsvp)<div class="list-row"><div><strong>{{ $rsvp->event?->title }}</strong><small>{{ $rsvp->event?->starts_at?->format('M j, Y g:i A') }}</small></div><span class="pill">{{ ucfirst($rsvp->status) }}</span></div>@empty<p>No RSVPs yet.</p>@endforelse</div></section>@endsection
