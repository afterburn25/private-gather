@extends('layouts.app') @section('title',$conversation->subject?:'Conversation') @section('content')<section class="shell section"><h1>{{ $conversation->subject ?: 'Conversation' }}</h1><div class="message-thread">@foreach($messages->reverse() as $m)<div class="message {{ $m->user_id===auth()->id()?'mine':'' }}"><strong>{{ $m->user?->display_name ?: 'System' }}</strong><p>{{ $m->body }}</p><small>{{ $m->created_at->diffForHumans() }}</small></div>@endforeach</div><form method="post" class="panel form-inline">@csrf<textarea name="body" required placeholder="Write a message"></textarea><button class="btn primary">Send</button></form></section>@endsection
