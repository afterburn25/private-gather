@extends('layouts.app')
@section('title',match($section){'onboarding'=>'Get Started','saved'=>'Saved','notifications'=>'Notifications','privacy'=>'Privacy Center','connections'=>'Connections',default=>'My Private Gather'})
@push('head')<link rel="stylesheet" href="{{ \App\Support\MountUrl::to('/assets/product-completion.css') }}">@endpush
@section('content')
<section class="pg-page-hero"><div class="pg-shell"><span class="pg-eyebrow">MY PRIVATE GATHER</span><h1>{{match($section){'onboarding'=>'Make Private Gather yours.','saved'=>'Your saved world.','notifications'=>'Stay informed without being overwhelmed.','privacy'=>'Privacy that follows your choices.','connections'=>'People you chose to connect with.',default=>'My Private Gather'}}</h1><p>{{match($section){'onboarding'=>'A short setup improves discovery while keeping sensitive account data out of your social profile.','saved'=>'Favorite events, followed clubs and saved searches in one place.','notifications'=>'Choose exactly which account, event, membership and message updates can reach you.','privacy'=>'Control profile visibility, location granularity, messaging and activity sharing from one screen.','connections'=>'Connections and follows are explicit. Blocking immediately removes the social connection.',default=>''}}</p><div class="pc-chip-row"><a href="{{route('saved.index')}}">Saved</a><a href="{{route('connections.index')}}">Connections</a><a href="{{route('notifications.index')}}">Notifications</a><a href="{{route('privacy.index')}}">Privacy</a><a href="{{route('member.security')}}">Security</a></div></div></section>
<section class="pg-page"><div class="pg-shell">
@if($section==='onboarding')<div class="pc-two"><form method="post" action="{{route('onboarding.save')}}" class="pg-card form-stack">@csrf<span class="pg-eyebrow">5-MINUTE SETUP</span><h2>Your social identity</h2><label>Display name<input name="display_name" value="{{old('display_name',auth()->user()->display_name)}}" required></label><label>Profile type<select name="profile_type"><option value="individual" @selected($profile?->profile_type==='individual')>Individual</option><option value="couple" @selected($profile?->profile_type==='couple')>Couple</option></select></label><div class="form-grid"><label>City<input name="city" value="{{$profile?->city}}"></label><label>Region<input name="region" value="{{$profile?->region}}"></label></div><label>Headline<input name="headline" value="{{$profile?->headline}}" placeholder="A little about your vibe"></label><label>Interests<input name="interests" value="{{implode(', ',$profile?->interests??[])}}" placeholder="Travel, socials, music, pool parties"></label><label class="row-actions"><input type="checkbox" name="discoverable" value="1" @checked($profile?->discoverable??true)> Allow this profile to appear in member discovery where community rules permit</label><button class="button button-primary">Finish setup</button></form><aside class="pg-card"><span class="pg-eyebrow">WHY THIS MATTERS</span><h2>Better recommendations, less exposure.</h2><p class="muted">Discovery uses broad city and interests. Date of birth, security credentials and exact private information remain account-only.</p><div class="pc-progress"><span style="width:{{$progress?->member_completed_at?100:25}}%"></span></div></aside></div>
@elseif($section==='saved')<div class="pg-section-head"><div><span class="pg-eyebrow">FAVORITES</span><h2>Events</h2></div><a class="text-link" href="{{route('discover.index')}}">Discover more →</a></div><div class="pg-grid pg-grid-3">@forelse($events as $event)<article class="pg-card"><span class="pg-pill">{{$event->starts_at->format('M j')}}</span><h3>{{$event->title}}</h3><p class="muted">{{$event->tenant->name}}</p><div class="pg-actions"><a class="button button-ghost" href="{{route('events.show',$event)}}">View</a><form method="post" action="{{route('favorites.destroy',['event',$event->id])}}">@csrf @method('DELETE')<button class="button button-ghost">Remove</button></form></div></article>@empty<div class="pg-empty">No saved events yet.</div>@endforelse</div><section class="pg-section"><h2>Clubs</h2><div class="pg-grid pg-grid-3">@forelse($clubs as $club)<article class="pg-card"><h3>{{$club->name}}</h3><div class="pg-actions"><a class="button button-ghost" href="{{route('clubs.show',$club)}}">Profile</a><form method="post" action="{{route('favorites.destroy',['tenant',$club->id])}}">@csrf @method('DELETE')<button class="button button-ghost">Remove</button></form></div></article>@empty<div class="pg-empty">No saved clubs yet.</div>@endforelse</div></section><section class="pg-section"><h2>Saved searches</h2><div class="pg-grid">@forelse($searches as $search)<div class="pg-card row-between"><div><strong>{{$search->name}}</strong><small>{{$search->alert_enabled?'Alerts enabled':'No alerts'}} · {{$search->search_type}}</small></div><form method="post" action="{{route('saved-searches.destroy',$search->id)}}">@csrf @method('DELETE')<button class="button button-ghost">Delete</button></form></div>@empty<div class="pg-empty">No saved searches.</div>@endforelse</div></section>
@elseif($section==='notifications')<div class="pc-two"><section class="pg-card"><div class="row-between"><div><span class="pg-eyebrow">INBOX</span><h2>Notifications</h2></div><form method="post" action="{{route('notifications.read-all')}}">@csrf<button class="button button-ghost">Mark all read</button></form></div><div class="pg-grid">@forelse($notifications as $note)@php($data=json_decode($note->data,true)?:[])<form method="post" action="{{route('notifications.read',$note->id)}}" class="pc-notification {{$note->read_at?'':'is-new'}}">@csrf @method('PATCH')<strong>{{$data['label']??str_replace('.',' ',ucfirst($note->type))}}</strong><small>{{\Carbon\Carbon::parse($note->created_at)->diffForHumans()}}</small><button>Mark read</button></form>@empty<div class="pg-empty">You're all caught up.</div>@endforelse</div>{{$notifications->links()}}</section><form method="post" action="{{route('notifications.preferences')}}" class="pg-card form-stack">@csrf @method('PATCH')<span class="pg-eyebrow">PREFERENCES</span><h2>Choose your channels</h2>@foreach(['email_events'=>'Event email','email_messages'=>'Message email','email_membership'=>'Membership email','email_tickets'=>'Ticket email','browser_notifications'=>'Browser notifications','push_messages'=>'Message push','push_events'=>'Event push','push_membership'=>'Membership push','push_tickets'=>'Ticket push','email_marketing'=>'Optional marketing email'] as $field=>$label)<label class="row-actions"><input type="checkbox" name="{{$field}}" value="1" @checked($prefs?->$field??($field!=='email_marketing'))>{{$label}}</label>@endforeach<label>Digest frequency<select name="digest_frequency"><option value="instant" @selected(($prefs?->digest_frequency??'instant')==='instant')>Instant</option><option value="daily" @selected(($prefs?->digest_frequency??'')==='daily')>Daily digest</option><option value="weekly" @selected(($prefs?->digest_frequency??'')==='weekly')>Weekly digest</option></select></label><button class="button button-primary">Save preferences</button></form></div>
@elseif($section==='privacy')@php($p=$privacy) <div class="pc-two"><form method="post" action="{{route('privacy.update')}}" class="pg-card form-stack">@csrf @method('PATCH')<span class="pg-eyebrow">VISIBILITY</span><h2>Who sees what</h2><label>Profile visibility<select name="profile_visibility">@foreach(['private'=>'Only me','connections'=>'Connections','members'=>'Eligible community members'] as $v=>$l)<option value="{{$v}}" @selected(($p?->profile_visibility??'members')===$v)>{{$l}}</option>@endforeach</select></label><label>Messages from<select name="messages_from">@foreach(['nobody'=>'Nobody new','connections'=>'Connections','members'=>'Eligible community members'] as $v=>$l)<option value="{{$v}}" @selected(($p?->messages_from??'connections')===$v)>{{$l}}</option>@endforeach</select></label><label>Location<select name="location_visibility"><option value="hidden" @selected(($p?->location_visibility)==='hidden')>Hidden</option><option value="city" @selected(($p?->location_visibility??'city')==='city')>City only</option><option value="region" @selected(($p?->location_visibility)==='region')>Region only</option></select></label><label>Club memberships<select name="memberships_visibility">@foreach(['private'=>'Private','connections'=>'Connections','members'=>'Members'] as $v=>$l)<option value="{{$v}}" @selected(($p?->memberships_visibility??'private')===$v)>{{$l}}</option>@endforeach</select></label><label>Event attendance<select name="attendance_visibility">@foreach(['private'=>'Private','connections'=>'Connections','members'=>'Members'] as $v=>$l)<option value="{{$v}}" @selected(($p?->attendance_visibility??'private')===$v)>{{$l}}</option>@endforeach</select></label><label>Online status<select name="online_visibility">@foreach(['hidden'=>'Hidden','connections'=>'Connections','members'=>'Members'] as $v=>$l)<option value="{{$v}}" @selected(($p?->online_visibility??'connections')===$v)>{{$l}}</option>@endforeach</select></label><label class="row-actions"><input type="checkbox" name="read_receipts" value="1" @checked($p?->read_receipts??true)> Send read receipts</label><label class="row-actions"><input type="checkbox" name="profile_view_receipts" value="1" @checked($p?->profile_view_receipts??false)> Participate in profile-view receipts</label><button class="button button-primary">Save privacy controls</button></form><section class="pg-card"><span class="pg-eyebrow">BLOCK LIST</span><h2>Blocked members</h2>@forelse($blocks as $block)<div class="row-between"><span>{{$block->display_name?:$block->name}}</span><form method="post" action="{{route('blocks.destroy',$block->blocked_id)}}">@csrf @method('DELETE')<button class="button button-ghost">Unblock</button></form></div>@empty<p class="muted">No blocked members.</p>@endforelse<div class="pg-privacy-note" style="margin-top:18px">Blocking removes an existing connection and prevents new connection requests between the two accounts.</div></section></div>
@elseif($section==='connections')<div class="pg-grid pg-grid-2">@forelse($connections as $connection)@php($other=(int)$connection->requester_id===auth()->id()?$connectionUsers->get($connection->addressee_id):$connectionUsers->get($connection->requester_id)) @if($other)<article class="pg-card"><div class="row-between"><div><span class="pg-pill">{{ucfirst($connection->status)}}</span><h3>{{$other->display_name?:$other->name}}</h3><p class="muted">{{$other->profile?->city?:'Location private'}} @if($other->profile?->headline)· {{$other->profile->headline}}@endif</p></div>@if($connection->status==='pending'&&(int)$connection->addressee_id===auth()->id())<div class="pg-actions"><form method="post" action="{{route('connections.respond',$connection->id)}}">@csrf @method('PATCH')<input type="hidden" name="decision" value="accept"><button class="button button-primary">Accept</button></form><form method="post" action="{{route('connections.respond',$connection->id)}}">@csrf @method('PATCH')<input type="hidden" name="decision" value="decline"><button class="button button-ghost">Decline</button></form></div>@else<form method="post" action="{{route('connections.destroy',$connection->id)}}">@csrf @method('DELETE')<button class="button button-ghost">Remove</button></form>@endif</div></article>@endif @empty<div class="pg-empty"><h3>No connections yet</h3><p>Connections are opt-in and separate from simply sharing a club.</p></div>@endforelse</div>
@endif
</div></section>
@endsection
