@extends('layouts.app')
@section('title','Users')
@section('content')<div class="container section"><h1>Users</h1><form class="form-inline"><input name="q" value="{{request('q')}}" placeholder="Search users"><button>Search</button></form>@foreach($users as $u)<form class="panel row-between" method="post" action="{{route('admin.users.update',$u)}}">@csrf @method('patch')<div><strong>{{$u->display_name?:$u->name}}</strong><small>{{$u->email}} · joined {{$u->created_at}}</small></div><div class="inline-controls"><select name="status">@foreach(['active','suspended','banned'] as $s)<option @selected($u->status===$s)>{{$s}}</option>@endforeach</select><label><input type="checkbox" name="is_platform_admin" value="1" @checked($u->is_platform_admin)> Private Gather Admin</label><button>Save</button></div></form>@endforeach{{$users->links()}}</div>@endsection